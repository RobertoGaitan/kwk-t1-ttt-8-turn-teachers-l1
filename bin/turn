#!/usr/bin/env ruby
require_relative '../lib/turn.rb'

puts "Welcome to Tic Tac Toe!"

board = Array.new(9) {" "}
input = 999
index = 999

display_board(board)
    puts "Please enter 1-9:"
turn(board)
turn(board)
turn(board)
turn(board)
turn(board)
turn(board)
turn(board)
turn(board)
turn(board)
